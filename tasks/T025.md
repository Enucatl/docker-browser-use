# T025 — End-to-end MVP smoke path and operator README

## Goal

Prove the Phase 1 vertical slice works in compose and document how to operate it on this homelab host.

## Context

MVP exit criteria from the plan: Browser Use ↔ Jev adapter, event/audit layer, persistent Chrome profile, simple web UI, human pause/takeover.

## Dependencies

- T015 through T024 (soft: mark blockers clearly if some are partial)

## In scope

1. Write an operator runbook in `README.md` (or `docs/operator.md`):
   - prerequisites (Traefik, Authelia, `DOCKER_DOMAIN`, secrets)
   - `docker compose up`
   - first login via Authelia
   - start a demo run (even against a static public page)
   - pause / approve / takeover
   - where audit data and artifacts live
   - hardening profile notes for browser
2. Add a scripted smoke checklist (`docs/smoke-checklist.md`) that a human or agent can follow.
3. Fix any glue gaps discovered while writing the smoke path (small only).
4. Record known limitations (fake Jev vs real Jev, Bitwarden not yet, etc.).

## Out of scope

- Phase 2 features
- Large refactors

## Acceptance criteria

- [ ] README enables a cold operator to bring the stack up behind Traefik/Authelia
- [ ] Smoke checklist covers audit verification (at least one SQL or UI check)
- [ ] CDP exposure explicitly verified absent

## Suggested files

```text
README.md
docs/operator.md
docs/smoke-checklist.md
```

## Implementation notes for the model

- Link to `task_ledger.md` for remaining work.
- Mention real path `../compose-security-baseline`.
